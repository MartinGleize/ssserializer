package ssserializer.json.compact
// the ssserializer.json.compact package provides the implicit compact JSON serializer/deserializer

import ssserializer.ApiTest

class CompactJsonApiTest extends ApiTest("Compact JSON") {
  // the ssserializer.json.compact package object provides the implicit compact JSON serializer/deserializer
  // the test cases come from the super class
}
